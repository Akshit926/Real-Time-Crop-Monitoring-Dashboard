{
  "name": "agrovision-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
